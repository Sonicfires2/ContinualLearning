"""Baseline experiment entry points.

Import concrete baselines from their modules, for example
`src.baselines.fixed_periodic_replay`. Keeping this package initializer light
avoids eager imports when running a baseline module with `python -m`.
"""
